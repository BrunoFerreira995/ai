# Phase 10 — Evaluation

Evaluation utilities included here:

- Accuracy, precision, recall, F1, and ROC AUC
- Confusion matrix
- False-positive and false-negative analysis
- Class distribution and imbalance detection
- Per-group metrics for dataset-bias checks

Run the tests:

```bash
.venv/bin/python -m unittest discover -s phase10_evaluation -p 'test_*.py'
```
