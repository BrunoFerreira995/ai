# Phase 11 — Explainability

This module provides:

- Grad-CAM heatmaps for convolutional models
- SHAP gradient explanations
- LIME image explanations
- Multi-head attention outputs and attention-weight matrices

Run the tests:

```bash
TF_CPP_MIN_LOG_LEVEL=2 .venv/bin/python -m unittest discover -s phase11_explainability -p 'test_*.py'
```
